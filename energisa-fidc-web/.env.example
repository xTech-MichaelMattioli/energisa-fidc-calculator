# Supabase (optional – leave blank for local-only calculation)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Worker Railway – URL do serviço Python de cálculo
# Deixar em branco para desabilitar o worker (cálculo não será disparado)
VITE_WORKER_URL=https://seu-worker.railway.app
